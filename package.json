{
  "name": "my-lambda-function",
  "version": "1.0.0",
  "description": "A basic Lambda function in TypeScript",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "typescript": "^4.8.4"
  }
}
